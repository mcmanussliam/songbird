[server]
hosts = 0.0.0.0:5232

[auth]
type = htpasswd
htpasswd_filename = /config/htpasswd
htpasswd_encryption = plain

[rights]
type = owner_only

[storage]
filesystem_folder = /data/collections

[logging]
level = info
