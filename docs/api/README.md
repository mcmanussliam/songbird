# docs/api/

Generated API documentation. This directory is populated by `cargo doc` — do not hand-edit
files here.

```bash
cd core && cargo doc --no-deps --open
```

Output lands in `core/target/doc/` (gitignored). Rendered docs for a release build are published
here for reference.
